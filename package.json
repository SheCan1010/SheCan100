{
  "name": "shecan-app",
  "version": "1.0.0",
  "private": true,
  "description": "SheCan - directory of independent Israeli businesswomen",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium chromium-headless-shell || true"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "^1.56.0"
  }
}
